import { PageHero } from '@/components/page-blocks';
import Link from 'next/link';
const posts = [{slug:'como-saber-se-veiculo-passou-por-leilao',title:'Como saber se um veículo passou por leilão?',cat:'Histórico veicular'}, {slug:'o-que-significa-gravame',title:'O que significa gravame?',cat:'Financiamento'}, {slug:'o-que-verificar-carro-usado',title:'O que verificar antes de comprar um carro usado?',cat:'Compra de veículos'}];
export default function BlogPage() { return <><PageHero eyebrow="Conteúdos" title="Informação para ajudar você a comprar melhor." description="Guias diretos para entender melhor o histórico e os sinais de um veículo usado."/><section className="section blog-section"><div className="blog-toolbar"><span className="eyebrow">Artigos recentes</span><div className="blog-categories"><span>Todos</span><span>Compra de veículos</span><span>Segurança</span><span>Financiamento</span></div></div><div className="blog-grid">{posts.map((post,index) => <article className={`blog-card ${index === 0 ? 'blog-featured' : ''}`} key={post.slug}><div className="blog-image"/><span className="eyebrow">{post.cat}</span><h2><Link href={`/blog/${post.slug}`}>{post.title}</Link></h2><p>Entenda os pontos que merecem atenção antes de tomar uma decisão.</p><Link className="text-link" href={`/blog/${post.slug}`}>Ler artigo <span>→</span></Link></article>)}</div></section></>; }