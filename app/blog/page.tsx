import Link from 'next/link';
import { PageHero } from '@/components/page-blocks';
import { posts } from '@/data/blog';

export const metadata = { title: 'Blog', description: 'Guias diretos para entender melhor uma compra de veículo.' };

export default function BlogPage() { return <><PageHero eyebrow="Conteúdos" title="Informação para ajudar você a comprar melhor." description="Guias diretos para entender o histórico e os sinais de um veículo usado."/><section className="section blog-section"><div className="blog-toolbar"><span className="eyebrow">Artigos recentes</span><div className="blog-categories" aria-label="Categorias"><span>Todos</span><span>Compra de veículos</span><span>Segurança</span><span>Financiamento</span></div></div><div className="blog-grid">{posts.map((post, index) => <article className={`blog-card ${index === 0 ? 'blog-featured' : ''}`} key={post.slug}><div className="blog-image"/><span className="eyebrow">{post.category}</span><h2><Link href={`/blog/${post.slug}`}>{post.title}</Link></h2><p>{post.excerpt}</p><small className="article-meta">{post.date}</small><Link className="text-link" href={`/blog/${post.slug}`}>Ler artigo <span>→</span></Link></article>)}</div></section><section className="simple-band"><span className="eyebrow">Conteúdo demonstrativo</span><h2>Vai comprar um veículo? Comece pela informação.</h2><Link className="button" href="/consultas">Ver consultas <span>→</span></Link></section></>; }
