import { NextResponse } from 'next/server';
import { prisma } from '@/lib/prisma';
import { requireSuperadmin } from '@/lib/auth';
export async function PATCH(request: Request, { params }: { params: { id: string } }) { const user = await requireSuperadmin(); if (!user) return NextResponse.json({ error: 'Acesso não autorizado.' }, { status: 403 }); const body = await request.json().catch(() => null); const data: Record<string, unknown> = {}; for (const key of ['priceCents', 'promotionalPriceCents', 'promotionStart', 'promotionEnd', 'description', 'shortName', 'useCase', 'ctaText', 'features', 'provider', 'providerProduct', 'providerEndpointKey', 'providerInputType']) if (key in (body || {})) data[key] = body[key]; if ('priceCents' in data && (!Number.isInteger(data.priceCents) || Number(data.priceCents) < 0)) return NextResponse.json({ error: 'Preço inválido.' }, { status: 400 }); if ('features' in data && !Array.isArray(data.features)) return NextResponse.json({ error: 'Características inválidas.' }, { status: 400 }); try { const product = await prisma.product.update({ where: { id: params.id }, data }); return NextResponse.json({ data: product }); } catch { return NextResponse.json({ error: 'Produto não encontrado ou não atualizado.' }, { status: 404 }); } }
