'use client';
import { useEffect, useState } from 'react';
type Config = { activeProvider: 'none' | 'mercadopago' | 'asaas'; environment: 'sandbox' | 'production'; mercadopago: { configured: boolean; publicKey: string; webhookConfigured: boolean }; asaas: { configured: boolean; webhookConfigured: boolean } };
const empty: Config = { activeProvider: 'none', environment: 'sandbox', mercadopago: { configured: false, publicKey: '', webhookConfigured: false }, asaas: { configured: false, webhookConfigured: false } };
export default function IntegrationsPage() { const [config, setConfig] = useState<Config>(empty); const [secrets, setSecrets] = useState({ mpToken: '', mpPublic: '', mpWebhook: '', asaasKey: '', asaasWebhook: '' }); const [message, setMessage] = useState(''); useEffect(() => { fetch('/api/admin/payment-providers').then(r => r.json()).then(body => setConfig(body)).catch(() => setMessage('Não foi possível carregar pagamentos.')); }, []); async function save() { const response = await fetch('/api/admin/payment-providers', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ activeProvider: config.activeProvider, environment: config.environment, mercadopago: { accessToken: secrets.mpToken, publicKey: secrets.mpPublic || config.mercadopago.publicKey, webhookSecret: secrets.mpWebhook }, asaas: { apiKey: secrets.asaasKey, webhookToken: secrets.asaasWebhook } }) }); const body = await response.json(); setMessage(response.ok ? 'Configuração de pagamentos salva.' : body.error || 'Não foi possível salvar.'); if (response.ok) { setConfig(body); setSecrets({ mpToken: '', mpPublic: '', mpWebhook: '', asaasKey: '', asaasWebhook: '' }); } } return <main className="admin-cms"><header className="admin-cms-header"><div><span className="admin-cms-kicker">SISTEMA / INTEGRAÇÕES</span><h1>Integrações de pagamento</h1><p>Configure os dois provedores e escolha apenas um como ativo.</p></div><button className="admin-cms-primary" onClick={save}>Salvar pagamentos</button></header>{message && <div className="admin-cms-message">{message}</div>}<section className="admin-panel-card"><label>Provedor ativo<select value={config.activeProvider} onChange={e => setConfig({ ...config, activeProvider: e.target.value as Config['activeProvider'] })}><option value="none">Nenhum</option><option value="mercadopago">Mercado Pago</option><option value="asaas">Asaas</option></select></label><label>Ambiente<select value={config.environment} onChange={e => setConfig({ ...config, environment: e.target.value as Config['environment'] })}><option value="sandbox">Sandbox/Teste</option><option value="production">Produção</option></select></label></section><div className="admin-product-editor-grid"><section className="admin-panel-card"><h2>Mercado Pago {config.mercadopago.configured ? '· configurado' : ''}</h2><label>Access Token<input type="password" value={secrets.mpToken} placeholder={config.mercadopago.configured ? 'Já configurado · informe para trocar' : 'APP_USR-...' } onChange={e => setSecrets({ ...secrets, mpToken: e.target.value })}/></label><label>Public Key<input value={secrets.mpPublic} placeholder={config.mercadopago.publicKey || 'TEST-...'} onChange={e => setSecrets({ ...secrets, mpPublic: e.target.value })}/></label><label>Webhook secret<input type="password" value={secrets.mpWebhook} placeholder={config.mercadopago.webhookConfigured ? 'Já configurado' : 'Opcional'} onChange={e => setSecrets({ ...secrets, mpWebhook: e.target.value })}/></label></section><section className="admin-panel-card"><h2>Asaas {config.asaas.configured ? '· configurado' : ''}</h2><label>API Key<input type="password" value={secrets.asaasKey} placeholder={config.asaas.configured ? 'Já configurada · informe para trocar' : '$aact_...'} onChange={e => setSecrets({ ...secrets, asaasKey: e.target.value })}/></label><label>Token do webhook<input type="password" value={secrets.asaasWebhook} placeholder={config.asaas.webhookConfigured ? 'Já configurado' : 'Opcional'} onChange={e => setSecrets({ ...secrets, asaasWebhook: e.target.value })}/></label></section></div></main>; }
