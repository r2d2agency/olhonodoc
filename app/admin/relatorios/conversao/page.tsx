import { prisma } from '@/lib/prisma';
export const dynamic = 'force-dynamic';
export default async function ConversionPage() { const [orders, grouped] = await Promise.all([prisma.order.findMany({ where: { status: { in: ['PAID', 'COMPLETED'] } }, select: { totalCents: true, amountCents: true } }), prisma.order.groupBy({ by: ['productId'], where: { status: { in: ['PAID', 'COMPLETED'] } }, _count: true, _sum: { totalCents: true } })]); const revenue = orders.reduce((sum, order) => sum + (order.totalCents ?? order.amountCents), 0); return <main className="admin-cms"><header className="admin-cms-header"><div><span className="admin-cms-kicker">RELATÓRIOS / CONVERSÃO</span><h1>Conversão</h1><p>Receita e pedidos pagos registrados na plataforma.</p></div></header><section className="admin-kpi-grid"><article className="admin-panel-card"><small>Pedidos pagos</small><strong>{orders.length}</strong></article><article className="admin-panel-card"><small>Receita registrada</small><strong>R$ {(revenue / 100).toFixed(2).replace('.', ',')}</strong></article><article className="admin-panel-card"><small>Ticket médio</small><strong>R$ {orders.length ? (revenue / orders.length / 100).toFixed(2).replace('.', ',') : '0,00'}</strong></article></section><section className="admin-panel-card"><h2>Conversão por produto</h2>{grouped.length ? <ul>{grouped.map(item => <li key={item.productId}>Produto {item.productId}: {item._count} pedido(s)</li>)}</ul> : <p>Nenhum pagamento confirmado ainda.</p>}</section></main>; }
